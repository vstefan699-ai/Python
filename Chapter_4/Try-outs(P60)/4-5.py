# Generate a list containing integers from 1 to 10
numbers = list(range(1, 11))

# min() scans the list and returns the smallest item (1)
print(min(numbers))

# max() scans the list and returns the largest item (10)
print(max(numbers))

# sum() calculates the total of all items in the list (55)
print(sum(numbers))