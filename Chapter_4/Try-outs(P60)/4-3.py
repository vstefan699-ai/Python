# The range() function generates a sequence of numbers.
# 1 is the starting point (inclusive).
# 21 is the stop point (exclusive), meaning it counts up to 20.
for number in range(1, 21):
    # Print the current number in the loop to the console
    print(number)