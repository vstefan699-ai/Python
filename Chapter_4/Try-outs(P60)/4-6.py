# Create a list of odd numbers using the step argument
# The arguments are: start=1, stop=21 (to include 20), and step=2
odd_numbers = list(range(1, 21, 2))

# Use a 'for' loop to iterate through each number in the list
for number in odd_numbers:
    # Print each odd number to the console
    print(number)